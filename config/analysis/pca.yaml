# Default configuration for PCA (Principal Component Analysis) feature importance.
# Usage: imp pca --config-name analysis/pca 'data/datasets=[...]'

vif:
  max_samples: null  # null = use all common dates; int = sample evenly
