# @package _global_
defaults:
  - /base
  - override /model: unet
  - _self_

# Reduce learning rate and increase weight decay to reduce overfitting
train:
  optimizer:
    lr: 1.5e-4
    weight_decay: 1.5e-3
