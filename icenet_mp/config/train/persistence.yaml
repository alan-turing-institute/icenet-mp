defaults:
  - default
  - override callbacks:
    - learning_rate
    - media_logging
    - metric_summary
    - unconditional_checkpoint
  - _self_

trainer:
  max_epochs: 1
  gradient_clip_val: null
