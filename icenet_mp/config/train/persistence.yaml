defaults:
  - default
  - override callbacks:
    - image_logging
    - learning_rate
    - metric_summary
    - unconditional_checkpoint
  - _self_

trainer:
  max_epochs: 1
  gradient_clip_val: null
