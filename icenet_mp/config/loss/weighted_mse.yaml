# =============================================================================
# Weighted MSE Loss
# =============================================================================
# Reference: https://pytorch.org/docs/stable/generated/torch.nn.MSELoss.html
#
# Mean squared error loss with per-element weighting. The loss is computed
# as elementwise squared error multiplied by sample_weights, then averaged
# to a scalar.
#
# When to use:
# - Regression tasks where spatial weighting is needed
# =============================================================================

_target_: icenet_mp.losses.weighted_mse_loss.WeightedMSELoss
