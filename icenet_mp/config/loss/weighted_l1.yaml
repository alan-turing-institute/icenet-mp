# =============================================================================
# Weighted L1 Loss
# =============================================================================
# Reference: https://pytorch.org/docs/stable/generated/torch.nn.L1Loss.html
#
# L1 loss with elementwise weighting. The loss is computed as elementwise
# L1 error multiplied by sample_weights, then averaged to a scalar.
#
# When to use:
# - Regression with outliers where spatial weighting is needed
# =============================================================================

_target_: icenet_mp.losses.weighted_l1_loss.WeightedL1Loss
